services:
  db:
    image: postgres:latest
#    container_name: postgres_db

    ports:
      - "5432:5432"

    restart: always

    environment:
#      POSTGRES_DB: postgres
#      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: shahl